// Re-export from shared module for backward compatibility
export { 
  createInitialState, 
  gameReducer, 
  enhancedGameReducer,
  createGameLogic,
  type GameLogicConfig 
} from '@shared/gameLogic';
